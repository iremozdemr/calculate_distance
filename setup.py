from setuptools import find_packages, setup

setup(
    name='calculate_distance',
    packages=find_packages(include=['calculate_distance']),
    version='0.1.0',
    description='my first python library',
    author='me',
    install_requires=[],
)